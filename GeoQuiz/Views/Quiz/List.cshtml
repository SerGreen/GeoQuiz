@model GeoQuiz.Models.Question

@{
    ViewBag.Title = "List";
}

<h2>Select flag of @Model.QuestionString</h2>

<div class="col-xs-12 well">
    <div class="col-xs-6 text-center">
        @using (Html.BeginForm("List", "Quiz"))
        {
            Html.Hidden("questionIndex", (int)ViewBag.QuestionIndex);
            Html.Hidden("answer", Model.Choices[0]);
            <input class="img-thumbnail" type="image" src="~/Content/Flags/@(Model.Choices[0]).png" name="submit" />
            <input type="submit" value="wtf" />
        }
        <br />
        @using (Html.BeginForm("List", "Quiz"))
        {
            Html.Hidden("questionIndex", (int)ViewBag.QuestionIndex);
            Html.Hidden("answer", Model.Choices[1]);
            <input class="img-thumbnail" type="image" src="~/Content/Flags/@(Model.Choices[1]).png" name="submit" />
        }
    </div>
    <div class="col-xs-6 text-center">
        @using (Html.BeginForm("List", "Quiz"))
        {
            Html.Hidden("questionIndex", (int)ViewBag.QuestionIndex);
            Html.Hidden("answer", Model.Choices[2]);
            <input class="img-thumbnail" type="image" src="~/Content/Flags/@(Model.Choices[2]).png" name="submit" />
        }
        <br />
        @using (Html.BeginForm("List", "Quiz"))
        {
            Html.Hidden("questionIndex", (int)ViewBag.QuestionIndex);
            Html.Hidden("answer", Model.Choices[3]);
            <input class="img-thumbnail" type="image" src="~/Content/Flags/@(Model.Choices[3]).png" name="submit" />
        }
    </div>
</div>